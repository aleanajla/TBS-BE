const user_router = require("./user_router")
const STID_router = require("./STID_router")
const booking_router = require("./booking_router")
const slot_router = require("./slot_router")

module.exports = {
    user_router,
    STID_router,
    booking_router,
    slot_router
}
